[defaults]
# Always prompt for the Ansible Vault password at runtime so the encrypted
# SSH private key(s) in roles/ssh_keys/files/ can be decrypted in memory.
# No vault password is ever stored in this repository.
ask_vault_pass = True
